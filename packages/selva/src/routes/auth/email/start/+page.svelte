<!--
	No UI — `load` always redirects, and POST is handled by the action in
	`+page.server.ts`. This file exists because SvelteKit requires a page
	component for routes with form actions.
-->
<svelte:head>
	<title>Email sign-in</title>
</svelte:head>
