{
	"name": "@selvajs/plugin-ui",
	"description": "Selva Grasshopper plugin UI (schema builder + preview), embedded into Selva.gha",
	"private": true,
	"version": "0.10.0",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"type-check": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:e2e": "playwright test"
	},
	"devDependencies": {
		"@lucide/svelte": "catalog:",
		"@playwright/test": "catalog:",
		"@selvajs/config": "workspace:*",
		"@sveltejs/adapter-static": "^3.0.10",
		"@types/three": "catalog:",
		"@types/ws": "catalog:",
		"tw-animate-css": "catalog:",
		"vitest": "catalog:",
		"ws": "catalog:"
	},
	"dependencies": {
		"@iconify/svelte": "catalog:",
		"@selvajs/compute": "catalog:",
		"@selvajs/schemas": "workspace:*",
		"@selvajs/ui": "workspace:*",
		"bits-ui": "^2.18.0",
		"mode-watcher": "catalog:",
		"svelte-dnd-action": "^0.9.69",
		"tailwind-variants": "^3.2.2",
		"three": "catalog:",
		"zod": "^4.3.6"
	}
}
