/**
 * Deliberately empty. Import from a subpath instead (`/core`, `/grasshopper`) —
 * the root would be a kitchen sink that promises Rhino/Grasshopper to every
 * consumer, including a future backend that has nothing to do with either.
 */

export {};
