{
  "name": "@computebuilder/schemas",
  "version": "1.0.0",
  "description": "Schema definitions and code generators for ComputeBuilder",
  "type": "module",
  "scripts": {
    "generate:ts": "node generate-typescript.js",
    "generate:cs": "node generate-csharp.js",
    "generate:all": "pnpm run generate:ts && pnpm run generate:cs"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.0"
  }
}
