{
	"name": "selva-monorepo",
	"version": "1.0.0",
	"description": "Selva - TypeScript/Rhino integration monorepo",
	"private": true,
	"type": "module",
	"repository": {
		"type": "git",
		"url": "https://github.com/vektornode/selva.git"
	},
	"bugs": "https://github.com/vektornode/selva/issues",
	"homepage": "https://github.com/vektornode/selva",
	"engines": {
		"node": ">=18.0.0",
		"pnpm": ">=9.0.0"
	},
	"scripts": {
		"dev": "pnpm --filter @selvajs/builder-app dev",
		"dev:builder": "pnpm --filter @selvajs/builder-app dev",
		"dev:compute": "pnpm --filter @selvajs/compute-app dev",
		"dev:ui": "pnpm --filter @selvajs/ui dev",
		"generate": "pnpm --filter @selvajs/schemas generate:all",
		"build:platform": "pnpm --filter @selvajs/platform build && pnpm --filter @selvajs/local-provider build && pnpm --filter @selvajs/supabase-provider build",
		"build:schemas": "pnpm --filter @selvajs/schemas build",
		"build:ui": "pnpm --filter @selvajs/ui build",
		"build:ui:fast": "pnpm --filter @selvajs/ui build:fast",
		"watch:ui": "pnpm --filter @selvajs/ui build:watch",
		"build:builder": "pnpm --filter @selvajs/builder-app build",
		"build:compute": "pnpm --filter @selvajs/compute-app build",
		"build:all": "pnpm build:platform && pnpm build:schemas && pnpm build:ui && pnpm build:builder && pnpm build:compute",
		"build:plugin": "pnpm build:schemas && pnpm build:ui && pnpm build:builder && node scripts/build-production.js",
		"check": "pnpm -r run --if-present check",
		"type-check": "pnpm -r run --if-present type-check",
		"lint": "eslint .",
		"lint:fix": "eslint . --fix",
		"format": "prettier --write \"**/*.{ts,tsx,svelte,json,yaml,md}\"",
		"format:check": "prettier --check \"**/*.{ts,tsx,svelte,json,yaml,md}\"",
		"clean": "node scripts/clean.js",
		"rebuild": "pnpm clean && pnpm install && pnpm build:all",
		"test": "pnpm -r run --if-present test",
		"changeset": "changeset",
		"release": "pnpm build:all && changeset publish"
	},
	"devDependencies": {
		"@changesets/cli": "^2.31.0",
		"@eslint/compat": "^2.0.5",
		"@eslint/js": "^9.39.4",
		"@selvajs/local-provider": "workspace:*",
		"@selvajs/platform": "workspace:*",
		"@selvajs/supabase-provider": "workspace:*",
		"@sveltejs/adapter-auto": "catalog:",
		"@sveltejs/kit": "catalog:",
		"@sveltejs/package": "^2.5.7",
		"@sveltejs/vite-plugin-svelte": "catalog:",
		"@tailwindcss/vite": "catalog:",
		"@types/node": "catalog:",
		"@types/three": "catalog:",
		"@typescript-eslint/eslint-plugin": "^8.59.0",
		"@typescript-eslint/parser": "^8.59.0",
		"eslint": "catalog:",
		"eslint-config-prettier": "^10.1.8",
		"eslint-plugin-svelte": "^3.17.1",
		"globals": "^17.5.0",
		"prettier": "catalog:",
		"prettier-plugin-svelte": "^3.5.1",
		"prettier-plugin-tailwindcss": "^0.7.3",
		"publint": "^0.3.18",
		"svelte": "catalog:",
		"svelte-check": "catalog:",
		"tailwindcss": "catalog:",
		"three": "catalog:",
		"typescript": "catalog:",
		"typescript-eslint": "^8.59.0",
		"vite": "catalog:"
	}
}
