# Embeddable Server Layer — Tracker

**Started:** 2026-07-08 (branch `beta`)
**Context:** Selva's published packages (`@selvajs/platform`, `@selvajs/supabase-provider`) tell consuming apps to build their own stores on the engine's interfaces — the Supabase client bundle even documents it: _"a consuming app keeps `public` for its own tables"_ ([client.ts:4-10](../../packages/providers/supabase/src/data/client.ts#L4-L10)). But the API never finished that thought: the schema is hardcoded, the error mapper and pagination helpers are package-private, and the entire compute/solve server stack lives inside `@selvajs/selva` — which is a deployable app with **no `exports` field at all**. Any app built on the engine has to copy that code and re-port every improvement by hand (gzip responses, client LRU cache, rate limiting, SSRF guard — all recent, all unreachable as a library).
**Goal:** finish the "build your own app on the engine" contract: schema-aware client bundle + exported store utilities (P1–P2), then extract the compute server stack into a published package that Selva's own app dogfoods (P3).
**How to use this doc:** work top to bottom; mark Status as you go — `☐ open` / `▶ in progress` / `✅ done` / `🧊 deferred`.

---

## P1 — `@selvajs/supabase-provider` hygiene (small, self-contained — one PR)

All in `packages/providers/supabase/src/data/`. Each item is engine cleanup on its own; together they make the provider usable for app-owned stores in `public`.

| Status | ID     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅     | **S1** | **Schema-aware `ClientBundle`.** `buildClientBundle` pins `db: { schema: 'selva' }` in all three `createClient` calls ([client.ts:57,64,82](../../packages/providers/supabase/src/data/client.ts#L57)). Add a schema option to `forRequest(ctx, { schema })` (default `'selva'`). Must preserve the fail-closed dispatch exactly (system → service-role, session token → JWT-header client, else anon; [client.ts:72-90](../../packages/providers/supabase/src/data/client.ts#L72-L90)) and re-key the per-request memoization `WeakMap<RequestContext, Map<schema, client>>` — the schema pin and the RLS boundary travel together, so this belongs inside the bundle, not as a caller-side `.schema()` idiom. |
| ✅     | **S2** | **Canonical `mapPostgrestError`.** A private `mapError` is copy-pasted in six stores in two inconsistent variants (full vs slim): `SupabaseProjectStore.ts:441`, `SupabaseOrgStore.ts:535`, `SupabaseDefinitionStore.ts:507`, `SupabaseShareLinkStore.ts:178`, `SupabaseInviteStore.ts:173`, `SupabaseComputeServerStore.ts:381`. None handles `PGRST116`. Create `src/data/errors.ts` exporting the superset (PGRST116 → 404, 23505/23503 → 409, else 500-wrap; keep the "full" variant preserving `details`/`hint`), migrate all six stores, export from the `./data` barrel.                                                                                                                                 |
| ✅     | **S3** | **Export the pagination helpers.** `toRange`, `orderColumn`, `nextCursorFromRange`, `encodeCursor`, `decodeCursor` in [pagination.ts](../../packages/providers/supabase/src/data/pagination.ts) are used by five internal stores but absent from every barrel. Re-export from `src/data/index.ts`.                                                                                                                                                                                                                                                                                                                                                                                                              |
| ✅     | **S4** | **`clientBundleFromEnv(env)`.** `SupabaseDataProvider.fromEnv` does the `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` parse but also demands `SELVA_AT_REST_KEY` and returns a full `IDataProvider` ([SupabaseDataProvider.ts:83-105](../../packages/providers/supabase/src/data/SupabaseDataProvider.ts#L83-L105)). Export a bundle-only factory without the at-rest-key requirement; refactor `fromEnv` to call it.                                                                                                                                                                                                                                                                          |

## P2 — Store-building utilities (functions, not a base class)

The per-store boilerplate every store repeats — soft-delete filtering, audit stamping, acting-org guard — should be published helpers. Deliberately plain functions: matches how the engine's own stores are written, and a published base class ossifies a 0.x package.

| Status | ID     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅     | **U1** | **`requireActingOrg(ctx)` in `@selvajs/platform`.** Pure context logic (return `ctx.actingOrgId` or throw `ProviderError(403)`); belongs next to `hasPermission` in [context.ts](../../packages/platform/src/context.ts).                                                                                                                                                                                                                                                                                                                    |
| ✅     | **U2** | **Snake_case row-stamp helpers in the supabase-provider.** Platform's `auditUpdate`/`auditSoftDelete` ([audit.ts:22-46](../../packages/platform/src/utils/audit.ts#L22-L46)) correctly emit camelCase domain fields — but stores write snake_case rows, so every store hand-writes `updated_at`/`updated_by: ctx.userId ?? null`. Export row-level equivalents (`stampUpdate(ctx)` → `{ updated_at, updated_by }`, plus soft-delete variant). Row-column naming is a provider concern, so these live in the supabase-provider, not platform. |
| ✅     | **U3** | **`notDeleted(query)` soft-delete filter.** The `.is('deleted_at', null)` idiom repeated across engine stores, exported as a query helper.                                                                                                                                                                                                                                                                                                                                                                                                   |

**P2 implementation notes:**

- **U2** shipped as [`rowStamp.ts`](../../packages/providers/supabase/src/data/rowStamp.ts) with `stampUpdate(ctx)` and `stampSoftDelete(ctx)`. `stampUpdate` returns `{ updated_by }` (or `{}`), **not** `{ updated_at, updated_by }` as the item tentatively sketched: the engine's update paths deliberately let the per-table `updated_at` trigger set the timestamp and only stamp `updated_by` (omitted on system contexts to satisfy the `on delete set null` FK). `stampSoftDelete` is the variant that writes `updated_at` explicitly (to equal `deleted_at`). Migrated all ten hand-written `updated_by` sites and both duplicated `stampToRow` copies across Org/Project/Definition.
- **U3** shipped as [`query.ts`](../../packages/providers/supabase/src/data/query.ts) and is exported for app-owned stores. Deliberately **not** force-applied to the engine's own chains: `.is('deleted_at', null)` there sits mid-fluent-chain (after `.eq(...)`, before `.order()`/`.maybeSingle()`), where wrapping in `notDeleted(...)` fractures the chain and reads worse. The deliverable is the named, exported helper.

## P3 — Extract the compute server stack into a published package

The highest-value slice of `packages/selva/src/lib/server/` and the one where improvements are landing fastest (gzip + Server-Timing on `/api/compute`, pre-solve cache diagnostics, warm-client LRU, `cachesolve` default, errored-solve cache flag — all within the last month, all app-internal today). New package (working name `@selvajs/server`), consumed by Selva's own app so it can't rot.

| Status | ID     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ✅     | **K1** | **Done (2026-07-08).** Scaffolded [`@selvajs/server`](../../packages/server/) (depends on `@selvajs/compute` + `@selvajs/schemas`; subpath `@selvajs/server/compute`). Moved all four leaf modules: `safe-url` + `transform-input` are pure verbatim moves; `computeLimits` became `resolveComputeLimits(env)` + pure `readPositiveInt`/`readBool` parsers (ambient `$env` → injected `EnvRecord`); `computeRateLimit` became `createComputeRateLimiter(config)` (window/max injected, honoring the B5 "state behind a small interface" constraint so a shared-store impl can slot in later). The app keeps [`computeLimits.ts`](../../packages/selva/src/lib/server/computeLimits.ts) as a thin `$env` shim re-exporting the named consts (zero consumer churn) and [`computeRateLimit.server.ts`](../../packages/selva/src/lib/server/computeRateLimit.server.ts) as the singleton-limiter binding; the compute route imports `safe-url`/`transform-input` from the package directly. 62 package tests (rate-limit + safe-url + transform-input, all moved out of the app). Full workspace type-check + tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ✅     | **K2** | **Done (2026-07-08).** Moved the warm-client cache into [`@selvajs/server` `createClientCache(config)`](../../packages/server/src/compute/client-cache.ts) — per-server `GrasshopperClient` + `SolveScheduler` LRU, config injected (limits/flags/`onDebugLog`), `rhinoTiming`/`solveMeta` preserved. Honored **both ADR 0004 obligations**: (D1) cache now keys on server **`id`**, not `serverUrl+apiKey`; `getClient` takes an opaque `ServerIdentity` (`serverIdentity()` derives it); `evict(id)`/`disposeAll()` added, and the app's [admin](../../packages/selva/src/routes/admin/api/compute/+server.ts) + [org](../../packages/selva/src/routes/api/org/compute/+server.ts) compute PUT routes call [`evictChangedServers`](../../packages/selva/src/lib/server/compute/evictChangedServers.ts) so a rotated/removed server drops its stale warm client (replacing the old implicit key-change staleness). (D2) `X-Selva-Definition` now rides on the wire — required a new `ComputeConfig.headers` hook in `@selvajs/compute` (the published client owned fetch with no header seam; added in `../selva-compute` + tests + changeset, merged UNDER auth headers so it can't clobber them), threaded through the solve route (guid) and the getIO render path (`record.guid`). App [`clientCache.server.ts`](../../packages/selva/src/lib/server/compute/clientCache.server.ts) is now a thin binding (env-derived config, `getClient`/`evictComputeClient` re-exports; zero consumer churn). 11 package tests + 8 app binding/eviction tests. **Publish gate: ✅ cleared (2026-07-11).** `@selvajs/compute` 3.1.0-beta.3 was published (dist-tag `beta`, 2026-07-08) and the shipped artifact verified to contain the headers hook (type declaration + `buildHeaders` merge under auth headers); the `link:../selva-compute` override was dropped from [package.json](../../package.json). Full workspace type-check + tests green against the registry package.                                                                                                                                                                                                                                                                                                             |
| ✅     | **K3** | **Done (2026-07-09).** Extracted the transport-agnostic solve pipeline out of [routes/api/compute/+server.ts](../../packages/selva/src/routes/api/compute/+server.ts). Two new package modules: [`remote-definition.ts`](../../packages/server/src/compute/remote-definition.ts) — `createRemoteDefinitionFetcher(config)` owning the SSRF-guarded (`assertSafeRemoteDefinitionUrl`) + size-capped + TTL-cached remote `.gh` fetch (`now` injected so the TTL is deterministic; `readBodyWithCap` moved verbatim); and [`solve-pipeline.ts`](../../packages/server/src/compute/solve-pipeline.ts) — `runSolvePipeline(args)` running input tree build → `scheduler.solve` (with abort/timeout classification) → JSON serialize (size-guarded) → gzip → Server-Timing envelope, returning a **discriminated `SolveOutcome`** (`ok` \| `timeout` \| `client_abort` \| `too_large` \| `compute_error`) so nothing throws for an expected failure. The route keeps ALL app policy (auth/share-token, `requireCanSolve`/`requireCanEditDefinition`, DB reads, share-cap increment, solve-count bump, schema backfill, metric sink) and maps each outcome to `apiError` (504/499/413) or re-throws `compute_error` for the existing 503/500 path. App binding: [`remoteDefinition.server.ts`](../../packages/selva/src/lib/server/compute/remoteDefinition.server.ts) (env-wired fetcher). **D5 resolved:** the pipeline's typed `SolveEnvelope` IS the contract; an additive `X-Selva-Compute-Version: 1` header (`COMPUTE_CONTRACT_VERSION`) makes it explicit/greppable with zero client churn (full Zod request/response SDK deferred to the P3 follow-on per the plan). 96 package tests (+34: 23 new pipeline/remote-definition). Q4/Q5 safety-net (`solve-cap-and-count`) + all 214 app tests pass unchanged. Full workspace type-check green. **Publish-gate carry-over: ✅ resolved (2026-07-11, see K2)** — the `buildHeaders` merge, changeset, and npm publish of 3.1.0-beta.3 were all confirmed; clean-checkout builds resolve from the registry.                                                                                                                                                                                                                              |
| ✅     | **K4** | **Done (2026-07-11).** Audit D1 decided first, as required: [ADR 0005](../adr/0005-uischema-version-and-disposable-schema-cache.md) — stored schemas are a **disposable cache**, compute (running the C# `SchemaMigrator`) is the only migration engine, the web never migrates in TS. Implemented in the app, then extracted as a pure move: new [`@selvajs/server/definitions`](../../packages/server/src/definitions/) subpath (package now depends on `@selvajs/platform`) exporting `DefinitionService` (verbatim — already DI'd on data+storage), `fetchSchemaFromCompute`/`SchemaExtractionError` (+ new `assertSupportedSchemaVersion` compat gate: newer-than-app schema format → `'unsupported'` → 422), and `createDefinitionLoader(deps)` (loadForRender with all wiring injected: stores, `resolveServer`, warm-client `getClient`, `fetchSchema`, `computeDebug`, `onWarn`). Cache-staleness check: cached schema used only when `schemaVersion === UI_SCHEMA_VERSION` (new generated constant; `schemaVersion` is now **required** in `ui-schema.json` — format 2.11.0→2.12.0 with a no-op `MigrateTo_2_12_0`); otherwise re-extract + best-effort persist-back (only at current version, so an outdated compute plugin can't thrash the cache). Supabase gets a `schema_version` GENERATED column (ops-only). App files are now thin shims/bindings (zero consumer churn). Fixed a latent bug the move surfaced: shallow `camelcaseKeys` no-ops on arrays, so the PascalCase `Schemas` wrapper was never normalized — now camelcased per element. 26 new package tests (122 total). `slug.ts` stayed (app-shaped naming policy); `api-errors.ts` stayed (SvelteKit-coupled by design). Full workspace type-check + tests + svelte-check green. ⚠️ C# suite not run locally (only .NET 10 runtime installed; net8.0 testhost won't start) — `Selva.Schema` compiles clean; rely on CI for `dotnet test`.                                                                                                                                                                                                                                                                                                                                                                |
| ✅     | **K5** | **Done (2026-07-11)** — thawed after K1–K4 proved the package shape. New [`@selvajs/server/providers`](../../packages/server/src/providers/create-selva-providers.ts) subpath: `createSelvaProviders(env, { registry, defaults?, configPath?, onBoot? })` → lazy `SelvaProviderRuntime` (`resolve`/`tenancy`/`branding`/`flag`/`solveMetricSink`). Key call: the provider set is a **caller-supplied `ProviderRegistry`** (choice name → `fromEnv` factory), so the package has zero dependencies on the provider implementations — a consuming app registers exactly what it ships. Everything else moved intact: `SELVA_CONFIG_PATH`-style external `selva.config.js` override (as `configPath`), lazy memoized instantiation (secrets untouched at import/build time), `SELVA_FLAG_*` parsing (compile-time-exhaustive over `SelvaFlags` via `satisfies` + never-check), branding defaults, tenancy, solve-metrics duck-type pick, boot summary (injectable `onBoot`). The app's [providers.server.ts](../../packages/selva/src/lib/server/providers.server.ts) is now pure composition root: registry of bundled providers, service singletons, Sentry + process hooks — all getters unchanged for consumers. 13 new package tests (135 total, incl. a real `configPath` fixture-module load). Full workspace green + live boot smoke: dev server 200 on `/`, `[selva] providers wired: auth=Local …` logged through the package runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 🧊     | **K6** | **Per-definition solve-cache control** (net-new feature, not an extraction). Today the two server-side caches — `cachesolve` (result cache) and `reuseServerDefinitionCache` (pointer/definition cache) — are **global env flags** ([computeLimits.ts](../../packages/selva/src/lib/server/computeLimits.ts)) baked into the warm client at create time. A definition can pin a **server** (`computeServerId`) but not toggle caching. **Blocked by the same wall as ADR 0004 D2:** the K2 warm client is shared across all definitions on a server (keyed by `id`), and `SolveScheduler.solve` takes only `{ signal }`, so a per-definition flag can't be a client-config value. Layered fix, bottom-up: **(1) `@selvajs/compute`** — add per-solve cache overrides to `SolveScheduler.solve` options (thread them into `applyOptionalComputeSettings` in [solve.ts](../../../selva-compute/src/features/grasshopper/solve.ts#L263), which today reads them off the client `config`); publish. **(2) `@selvajs/server`** — pass the per-solve flags through `getClient`→`scheduler.solve` (client stays shared; the flag rides the call, like `X-Selva-Definition` should have but couldn't). **(3) app** — add optional `cachesolve?`/`reuseDefinitionCache?` to the definition schema/record ([schemas.ts](../../packages/platform/src/definitions/schemas.ts), [types.ts](../../packages/platform/src/definitions/types.ts)), resolve `definition flag ?? global env default` in the solve route, plus a UI toggle. **✅ Design conversation happened (2026-07-11) — decisions banked, implementation not started:** (a) **scope = result caches only** (in-process L1 + Rhino `cachesolve` now, H1's durable L2 later) — pointer reuse stays global/per-server (it's an upload optimization, not a result cache; its risk is a property of the server, not the definition), so the sketch's `reuseDefinitionCache?` record field is **dropped**; (b) **flag home = definition record + settings UI** (editors), not version metadata or schema flag; (c) **shape = a single number** (`solveCacheLimit`: absent = inherit global env default, `0` = off, `N` = per-definition entry quota with LRU inside the definition — revised 2026-07-11 from the briefly-decided `'inherit' | 'off'`enum: the quota model gives per-definition cache isolation, and`0`subsumes the off-switch), resolved as`definition ?? global env default` — NOT per-cache booleans; a global byte budget (`SOLVE_CACHE_MAX_TOTAL_MB`) backstops memory since entry sizes vary KB→100s of MB; (d) **invalidation is structurally free** (append-only versions → new bytes/versionId → new keys everywhere; confirmed); (e) **channel-gating rule, tightened by the 2026-07-11 revalidation to ONE RESULT CACHE PER CHANNEL** (see the H1 amendments in `selva-compute/CACHING.md`): live solves run `{ cache: false }`(skip scheduler L1, use only the durable L2) while draft solves use only L1 and send`cachesolve: false` — so these per-solve flags are REQUIRED infrastructure for the L2 work, not optional hygiene; the DC byte cache keeps caching drafts (content-safe, editor iteration is its hottest use). Sequence with/after audit B6 (`maxConcurrent` — changes the load math). Layer plan (1)→(2)→(3) above stands with (3) amended per (a)–(c). |

## P4 — Second-wave reusable utilities (scanned 2026-07-11)

Full sweep of `packages/selva/src/lib/server/` + `hooks.server.ts` + reusable-looking routes for anything a **second Selva-like app** (SvelteKit on `@selvajs/platform` + providers + `@selvajs/compute`) would otherwise copy-paste — the K4–K6 slices were excluded (already tracked). Judgment lens: genuinely reusable across such apps only; app policy stays. `@selvajs/server` today ships only the `/compute` subpath, so everything here is a new subpath.

| Status | ID     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅     | **E1** | **HMAC token codec → `/tokens`.** [shareLinks/token.server.ts](../../packages/selva/src/lib/server/shareLinks/token.server.ts) and [invites/token.server.ts](../../packages/selva/src/lib/server/invites/token.server.ts) are near-identical (`mintRawToken`/`hashToken`/`hashesEqual`/`looksLike*Token`, differing only in prefix + env-var name) — two in-repo copies already prove the duplication. Extract as `createTokenCodec({ prefix, secret })` (K1 env-injection pattern); both call sites collapse to two instantiations. Deps: `node:crypto` only. **Highest value-to-cost in the set.** Natural moment to also land audit S3 (min secret length) once, in the factory.                                                                                                                                                                                     |
| ✅     | **E2** | **`SentryErrorReporter` → `/errors`.** [SentryErrorReporter.ts](../../packages/selva/src/lib/server/errors/SentryErrorReporter.ts) depends ONLY on `@selvajs/platform` types (`IErrorReporter`, `ErrorContext`); DSN/environment/release already constructor args; `@sentry/node` already optional + dynamic-imported. **Literal file move, zero refactor.** Only decision: list `@sentry/node` as an optional peer dep of the package.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ✅     | **E3** | **HTTP-hardening trio → `/http` (+ one dedupe).** From [admin-auth.server.ts](../../packages/selva/src/lib/server/admin-auth.server.ts): `safeRedirectTarget` (:145, open-redirect guard, zero deps — pure move) and `requireMaxBodySize` (:126 — small refactor: drop the kit `error()` for a transport-agnostic result the route maps). From [hooks.server.ts:446](../../packages/selva/src/hooks.server.ts#L446): `applySecurityHeaders` (parameterize prod detection; the deliberate CSP/frame-ancestors omissions for iframe embedding are documented and travel with it). **Dedupe, not a move:** admin-auth's login rate limiter (:25-53) is a redundant second implementation — delete in favor of K1's `createComputeRateLimiter` (superset). Cookie session I/O in the same file is **app-only** (names/TTLs/`Cookies` transport = session policy) and stays. |
| ✅     | **E4** | **Route-classifier factory → `/http`.** The deny-by-default classification model in [hooks.server.ts:154-212](../../packages/selva/src/hooks.server.ts#L154-L212) (`isStaticAsset`/`isPublicRoute`/`isSelfGatingApiRoute` over allowlist sets + prefix arrays + a self-gating exception) is a reusable _pattern_ with app-specific _values_. Extract `createRouteClassifier({ publicPages, publicPrefixes, publicApis, selfGatingPrefix, staticPrefixes })`; the app supplies its route sets. `handle`/`buildContext`/`handleError` stay app-side (provider-soaked). Touches the hooks hot path — land while the T1 CI gate is green.                                                                                                                                                                                                                                   |
| ✅     | **E5** | **Access-input assembly → `/access` (split access.server.ts).** [access.server.ts](../../packages/selva/src/lib/server/access.server.ts) is two layers: the ~20 `require*`/`assert*` gates (kit `error`/`redirect`, hardcoded `/admin`/`/library` targets — **app-only**, stays) and the rule-input marshalling `buildProjectAccessInput` (:158) / `projectAccessInputFromRows` (:209) that assembles ctx + provider rows + feature flags into what platform's pure `canView`/`canEdit`… rules consume. The "which rows does each visibility need" knowledge (:170-186) is exactly what a second app reimplements wrong. Refactor Layer B to take an injected provider bundle + flags reader. **Messiest split in the set — coordinate with K4** (shares `api-errors`).                                                                                                 |
| ✅     | **E6** | **Channel-aware semver → `/ops`.** `parseSemver`/`isNewer` in [updateCheck.server.ts:31-66](../../packages/selva/src/lib/server/updateCheck.server.ts#L31-L66) (stable+beta ordering) are pure and reusable — lift on next touch. The REST of the ops stack stays deliberately: registry URL/package name (`@selvajs/selva`), `selva-channel.json`, pm2 topology, `bootHealth`'s `verifySecrets` coupling are Selva-deployment policy, not engine building blocks.                                                                                                                                                                                                                                                                                                                                                                                                      |

**Scanned and ruled out (don't re-scan):** `shareLinks/resolve.server.ts` (bound to the ShareLink store + definition/channel semantics; the reusable part is E1), the `api/files` blob proxy (the substance — `classifyAssetPath` + asset-class registry — already lives in `@selvajs/platform`; the route is thin HTTP binding), the self-update runner (pm2/bash deployment topology baked in), `OrgAssetService` (clean + already DI'd but platform-domain glue — if it moves anywhere, push _down_ into `@selvajs/platform`, not sideways), `permissions.server.ts` + `admin-config.ts` (pure wiring shims). Marginal, parked: `permissions-compat.server.ts` (transitional by its own header comment), `org-members.server.ts` `listAllOrgMembers` (real but one function — better as a generic page-drain helper in platform's pagination if it recurs).

**Sequencing:** E1+E2+E3 have no prerequisites and fit one small PR (E2 is a literal move). E4 is small but hot-path. E5 waits for/rides with K4 (✅ done — E5 is unblocked). E6 rides along whenever `updateCheck` is next touched. None of these block or are blocked by the K-items.

**E1–E3 implementation notes (2026-07-11, one PR as planned):**

- **E1** shipped as [`@selvajs/server/tokens`](../../packages/server/src/tokens/token-codec.ts): `createTokenCodec({ prefix, secret })` → `{ mintRawToken, hashToken, hashesEqual, looksLikeToken }`. Audit S3 landed in the factory: `MIN_TOKEN_SECRET_LENGTH = 32` enforced at creation (a short dev secret now fails loudly on first token op instead of reaching production silently). Both app modules are thin env bindings — lazy codec resolution re-keyed on the secret so per-scenario env mutation in tests still works, and `looksLike*Token` stays a bare prefix check so a missing secret can't turn "is this even a share token?" into a 500. Note: S3's other half (`SELVA_HMAC_KEY` presence-only check in `LocalAuthProvider`) is NOT covered here — sessions don't go through the codec.
- **E2** shipped as [`@selvajs/server/errors`](../../packages/server/src/errors/SentryErrorReporter.ts) — the literal move it promised; `@sentry/node` is an optional peer dep (`peerDependenciesMeta`). Only consumer (`providers.server.ts`) repointed; app file deleted.
- **E3** shipped as [`@selvajs/server/http`](../../packages/server/src/http/): `safeRedirectTarget` (pure move, its 18-case test suite moved with it), `declaredBodySizeExceeds(headers, maxBytes)` (the transport-agnostic result; the app's `requireMaxBodySize` maps it to kit's 413), and `applySecurityHeaders(response, { hsts })` (hardening set + documented CSP/frame omissions; cache-control stayed in `hooks.server.ts` — asset-path layout is app policy). The dedupe required making the K1 limiter a true superset first: `ComputeRateLimiter` gained `peek(key)` (gate without spending budget) and `clear(key)` (forgive on success), then admin-auth's hand-rolled Map limiter collapsed to three one-line bindings over a package limiter instance. Cookie session I/O stayed app-side as planned. 36 new package tests (171 total); app suite 196 (the 18 moved redirect tests left). Full workspace type-check + tests green; changeset `server-second-wave-utilities`.

**E4–E6 implementation notes (2026-07-11, same day, second PR-sized slice):**

- **E4** shipped as [`route-classifier.ts`](../../packages/server/src/http/route-classifier.ts) in `/http`: `createRouteClassifier({ publicPages, publicPrefixes, publicApis, selfGatingPrefix, staticPrefixes, staticPaths })` (the sketch's five keys + `staticPaths` for exact assets like `/robots.txt`). Every allowlist is optional and an empty config gates everything — deny-by-default is structural. The app's `hooks.server.ts` keeps its route VALUES inline (and keeps `PUBLIC_PAGE_ROUTES` as a named const — the best-effort session attach distinguishes public _pages_ from prefixes/APIs, which the classifier deliberately doesn't model) and re-exports the three predicates from the classifier instance, so the 37-test route-classification suite passed **unchanged** — that suite is the hot-path safety net the item asked for, plus a live boot smoke (public 200 / health 200 / gated page 303→login / gated API 401 / static 200).
- **E5** shipped as [`@selvajs/server/access`](../../packages/server/src/access/project-access-input.ts): `createProjectAccessInputBuilder(deps)` owning `buildProjectAccessInput` + `projectAccessInputFromRows` — the per-visibility row-fetch knowledge moved verbatim (platform→grants gated on the flag, private→member, org/public→member+orgMember with the cross-org-public skip). Deps are **functions**, not provider objects (`getProjectMember`/`getOrgMember`/`listPlatformGrants` + a per-call `flags()` reader), so the app's lazily-initialized providers and runtime flag reads work unchanged; [access.server.ts](../../packages/selva/src/lib/server/access.server.ts) keeps all ~20 kit-coupled gates and binds the builder in ~15 lines. Less messy than predicted: the split didn't touch `api-errors` after all (that stayed with the gates). 11 new package tests pin the visibility→rows matrix.
- **E6** shipped as [`@selvajs/server/ops`](../../packages/server/src/ops/semver.ts): `parseSemver`/`isNewer` pure move; the package now owns the `ReleaseChannel` type (`releaseChannel.server.ts` re-exports it, one definition). `updateCheck.server.ts` re-exports `isNewer` for compat; its 5 `isNewer` tests moved to the package, the `fetchLatestVersion` failure-tolerance tests stayed app-side.
- Totals after E4–E6: 193 package tests (16 files), 191 app tests. Full workspace type-check + tests + lint + svelte-check green; changeset `server-second-wave-utilities` updated to cover all six subpaths. **P4 is complete.**

## Relationship to the pre-scale audit

[data-access-efficiency-audit.md](./data-access-efficiency-audit.md) is the findings tracker for the same code; keep them separate (it converges to done, this is roadmap work) but sequence around it:

- Audit ✅2a/✅2b already fixed the hot paths in `clientCache.server.ts` and the solve route — K2/K3 move **already-fixed** code, so land any further open audit fixes touching those files _before_ extracting them; moving and rewriting in one diff is unreviewable.
- **S2 here and audit §4b share the same six store files** — when migrating each store onto `mapPostgrestError`, replace its `select('*')` list paths with explicit columns in the same pass.
- Audit **D5** (unversioned `/api/compute` contract) resolves here: implement the version + response envelope **as part of K3** — the envelope is the extracted pipeline's typed return value, so designing it separately would version the contract twice. See "API contract evolution" below.
- Audit **B5** (in-process rate limiter / caches vs multi-instance) becomes a design constraint on K1/K2: keep the state behind small interfaces so a shared-store impl can slot in later without an API break.
- Audit **LB** (load-balancing groundwork: server identity as an **id**, not a URL; definition-guid routing header) must be decided **before K2 publishes** — the client-cache LRU key (`serverUrl+apiKey` today) becomes public API surface; switching the identity scheme afterwards is a semver break. Cheapest form: K2 exports `getClient(serverIdentity)` where the identity type is opaque from day one.
- Audit **D1** (UISchema versioning / migrate-on-read for stored blobs) lands in exactly the code K4 extracts (`DefinitionService` / `loadForRender`). **Decision made 2026-07-11** (stored schema = disposable cache; re-extract from compute on version mismatch; `schemaVersion` required at format 2.12.0 with `UI_SCHEMA_VERSION` exported — changeset `schema-version-required`). Remaining gate: implement the version check + re-extract in `loadForRender` **before** K4 moves it, so the package extracts the finished shape.
- Audit **Q4/Q5**'s compute-route test cases (share-cap at the endpoint, concurrent solve-count increments) double as the **extraction safety net**: written against the route _before_ K3, they must keep passing unchanged across the pipeline/adapter split.

## API contract evolution

As the HTTP surface grows, the contract should be a deliberate artifact, not ad-hoc SvelteKit routes:

- **Typed client SDK** (P3 follow-on): export a typed client from the same package, types derived from the existing Zod route schemas (or OpenAPI generated from them). This is the same single-schema philosophy as `ui-schema.json` → TS + C#, and it resolves audit D5 by making the compute contract an explicit, versionable type.
- **GraphQL: explicitly deferred.** The core endpoint is RPC-shaped (inputs → solve → results/geometry), the entity graph is shallow, and GraphQL would forfeit the transport-level wins (gzip + Server-Timing, binary payloads, HTTP caching) while adding query-complexity/security work. K3's transport-agnostic pipeline keeps the door open: any transport can wrap the package later if a real multi-client need appears. Revisit only when there are several third-party consumers composing queries.

## Implementation order

The couplings above, flattened into the sequence to actually work:

1. **P1 PR** (S1–S4) — no prerequisites. While migrating each of the six stores onto `mapPostgrestError` (S2), fix that store's `select('*')` columns in the same pass (audit 4b). Audit 4f's minor provider cleanups can ride along.
2. **P2 PR** (U1–U3) — independent of everything; do with or right after P1.
3. **Extraction gates** — three small things before any K-item ships. **✅ all cleared (2026-07-08):**
   - ✅ **LB identity ADR** written: [ADR 0004](../adr/0004-compute-server-identity-and-lb-affinity.md) — server identity = opaque `id` (not URL); solves route by definition-guid affinity. Decision only. Its two concrete K2 obligations: change the client-cache key `serverUrl+apiKey` → `id` with an explicit config-write eviction hook, and export `getClient(serverIdentity)` with an opaque identity type.
   - ✅ **Q4/Q5 compute-route tests** written. Sequential cap→null→429 mapping + loss-free counting against the local provider in [solve-cap-and-count.test.ts](../../packages/selva/src/routes/api/compute/__tests__/solve-cap-and-count.test.ts); the concurrent no-overshoot guarantee (a Supabase-RPC atomicity property the local whole-file store can't provide) in [share-link-concurrency.test.ts](../../packages/providers/supabase/src/data/__tests__/share-link-concurrency.test.ts), skipped without a live stack. These are the safety net that must pass unchanged across K3.
   - ✅ **Audit sweep — no-op by design.** The two hot-path compute audit items (§2a client-cache extraction, §2b double-`getProject`) are already done. The remaining compute-path items are deferred _into_ the K-work, not pre-extraction cleanup: **D5** (contract version + envelope) lands _as part of K3_ as the pipeline's return type; the **getIO cache-per-`fileKey`** follow-up is an addition (belongs in K2), not a move. The two `BRIDGE: remove ~2026-09` shims in the route are live migration code (removal date is future) and correctly stay. Conclusion verified: the K1–K3 extraction diffs will be pure moves.
4. **K1 → K2 → K3** in order. K1/K2 honor the B5 constraint (state behind small interfaces); K2 exports `getClient(serverIdentity)` with the identity type opaque; K3 implements the D5 version + envelope as the pipeline's return type and must keep the step-3 route tests green unchanged.
5. **K4 gate half-cleared:** audit D1 is **decided** (2026-07-11 — disposable-cache model, `schemaVersion` required at 2.12.0, `UI_SCHEMA_VERSION` exported); what still blocks K4 is _implementing_ the migrate-on-read check in `loadForRender` so the package extracts the finished shape, not a pre-D1 snapshot. **K5 stays 🧊** until the package shape has survived K1–K3.

Not blocking anything here: audit 2d–2h, 4a/4c–4e/4g, S3–S6, O-items, V-items, P1(privacy), D2–D4 — pick up in normal course of work.

## Non-goals

- **No published base class for stores** — helpers only (see P2 rationale).
- **No bolting `exports` onto `@selvajs/selva`** — app internals are entangled with `$env`/`$app` and would become an accidental semver surface. The library is the new package.
- **No third-party store registration into `SelvaConfig`/`IDataProvider`** — the engine's data contract stays the engine's; apps compose their own stores beside it. The public story is `clientBundleFromEnv` + `forRequest(ctx, { schema: 'public' })` + the P2 helpers, which gives app stores the same RLS/context discipline as engine stores for free.

## Sizing

P1 is hours, not days, and is a pure-win refactor (S2 alone deletes five duplicated mappers). P2 is similar. P3 is the real extraction — K1/K2 are mechanical once env injection is threaded; K3 is the careful one (the 589-line handler needs a policy/pipeline split); K4–K5 wait until the package boundary has settled against real usage.
