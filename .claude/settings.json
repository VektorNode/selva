{
  "permissions": {
    "allow": [
      "Bash(pnpm test *)",
      "PowerShell(docker exec supabase_db_selva-supabase-provider psql -U postgres -d postgres -c \"SELECT guid, project_id, owner_id, display_name, status FROM public.definitions;\")",
      "PowerShell(docker exec supabase_db_selva-supabase-provider psql -U postgres -d postgres -c \"SELECT project_id, user_id, role FROM public.project_members;\")",
      "PowerShell(docker exec supabase_db_selva-supabase-provider psql -U postgres -d postgres -c \"SELECT org_id, user_id, role, permissions FROM public.org_members;\")"
    ]
  }
}
