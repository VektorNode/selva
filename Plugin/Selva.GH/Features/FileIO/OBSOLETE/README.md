# OBSOLETE — do not delete

These are **not dead code**. `OBSOLETE_<Component>_UntilV<X_Y_Z>.cs` files are frozen
copies of retired component versions, and the `GH_*Upgrader.cs` files migrate them
forward, so Grasshopper documents saved with older Selva versions still open and
upgrade cleanly. See STRUCTURE.md ("OBSOLETE" convention) for the naming rules.
