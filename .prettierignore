# Build outputs
**/dist
**/build
**/.svelte-kit
**/coverage

# Dependencies
node_modules/
**/node_modules

# Generated files
**/*.min.js
**/*.min.css
**/.turbo
**/.next

# Package files
**/package-lock.json
**/pnpm-lock.yaml
**/yarn.lock

# Examples (have their own config)
examples/

# Build outputs
.next
build
dist

# Generated by scripts/fetch-server-contract.mjs, which writes tab-indented JSON
# with one array element per line. Prettier collapses those arrays, so formatting
# it here would just be undone by the next regeneration — and fail format:check
# in between.
packages/compute/tests/contract/server-contract.snapshot.json

# Generated by scripts/generate-openapi.js. A conformance test fails when this
# file drifts from the generator's output, so Prettier's quote style would break
# `pnpm test` on every regeneration.
packages/selva/openapi/v1.yaml

# Local test-run artifacts (untracked, but present in a dev working tree and
# otherwise picked up by the root format:check glob).
**/test-results
**/.e2e
packages/selva/e2e/.auth
