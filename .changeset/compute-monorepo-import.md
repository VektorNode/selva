---
'@selvajs/compute': patch
---

Move `@selvajs/compute` into the Selva monorepo (`packages/compute`). No API or behavior changes — the package continues to publish to npm under the same name and version line. Development, issues, and the SLVA mesh wire-format now live alongside the plugin and app that consume it.
