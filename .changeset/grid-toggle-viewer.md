---
'@selvajs/ui': minor
---

Add grid toggle to viewer tools menu. The grid can now be shown/hidden at runtime via a new `showGridToggle` prop (defaults to `true`). Grid starts hidden by default for a cleaner initial viewport. Hidden viewer helper objects (grid, floor, labels, measurement overlay) are now filtered from the scene object list.
