---
'@selvajs/ui': minor
---

Add `onValueChange` to `ClientSlotArgs`, letting a client slot commit a value back into the solve session like any built-in widget. Slots can now be interactive controls (e.g. a custom picker), not just display cells.
