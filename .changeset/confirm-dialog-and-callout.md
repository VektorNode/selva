---
'@selvajs/ui': minor
---

Add `ConfirmDialog` and `Callout` primitives, and guard destructive actions behind confirmation

`AppShell` and `SideNav` gain navigation and branding slots, and `ComputeApp` wires them through.
